<!DOCTYPE html>

<title>Edit UserData</title>

{% block body %}
    <h1>Edit UserData</h1>

    {{ include('user_data/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_user_data_index') }}">back to list</a>

    {{ include('user_data/_delete_form.html.twig') }}
{% endblock %}
